````markdown
# 통신 명세서 [작업번호]

> 데이터 흐름도 link: <데이터 흐름도 link>

## 채널 정의

| 채널 | 용도 | 페이로드 봉투 | 인증 |
|---|---|---|---|
| <채널명> | ... | `{cmd, [cmd]: <스키마>}` | ... |

## API endpoint 카탈로그

> `/<도메인명>/<리소스>` 규약. 도메인명은 이 작업의 도메인(Notion 작업 row의 `도메인` select 값)을 기준으로 한다. 상태: `[확정]` 또는 `[논의중]`.

### <METHOD /도메인/리소스> [확정 | 논의중]

- 사용 액션 ID: `<페르소나>:Action:N`
- Request:
  ```json
  { ... }
  ```
- Response:
  ```json
  { ... }
  ```
- 에러: 4XX/5XX 케이스

(각 endpoint 반복)

## Socket cmd 카탈로그

> 채널 위에서 디스패치. 상태: `[확정]` 또는 `[논의중]`.

### <cmd 명> [확정 | 논의중]

- 사용 ID: `<페르소나>:Action:N` 또는 `<페르소나>:Event:N`
- 발신자: 클라이언트 / 서버
- Payload:
  ```json
  { ... }
  ```
- 비고: ...

(각 cmd 반복)

## 수정사항

회의 결과 기록.
````
